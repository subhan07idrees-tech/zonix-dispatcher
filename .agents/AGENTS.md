# Project Rules

- For any UI/frontend work, always apply the `design-quality` skill before writing styling code.
- **Zero-Regression Rule:** When updating or editing UI, styling, or feature code, ALWAYS inspect backend API endpoints, data response property keys (`proxies`, `users`, `cookies`, `dashboard`), and handler signatures first. NEVER break or disconnect existing features, data rendering, or control buttons when making UI changes.
